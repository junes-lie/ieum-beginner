# 이음 Cloud Work 인수인계

## 기준

- 현재 저장소의 Linen 디자인이 시각 기준이다. 다른 프로토타입의 색, 레이아웃, 카피를 복사하지 않는다.
- 곡마다 다른 이미지 스킨을 입은 카세트와, 카세트에서 분리된 J카드를 유지한다.
- J카드의 두 트랙은 서로 다른 한국어 손글씨로 기록한다.
- 얼굴 사진은 쓰지 않는다. 음색은 SVG 아이콘이 아니라 래스터 이미지로 표현한다.
- 기획 문구를 슬로건처럼 노골적으로 반복하지 않는다. 다만 사용자가 무엇을 하고 무엇을 도움받는 앱인지는 명확히 안내한다.
- 일반적인 1:1 매칭이 아니다. 사용자가 곡 1개와 파트 1개를 녹음한 뒤, 빈 파트와 잘 맞는 여러 후보에게 제안한다. 각 후보는 상대 정보와 녹음을 보고 자유롭게 결정한다. 같은 파트에 여러 명이 참여하면 참여자마다 별도의 프로젝트가 생긴다.
- 완성 후 사용자가 듣기 좋았는지 하트로 평가하고 이후 추천에 반영한다.
- 서버는 제외 대상이 아니다. 실제 제품에 필요하면 프런트엔드, API, DB, 파일 저장소, 비동기 작업까지 구현한다.

## 현재 구현

- 정적 모바일 웹 UI와 Linen 디자인 시스템
- 최초 1회 음역 측정 흐름과 Web Audio 기반 음높이 감지
- 실제 마이크 녹음 및 브라우저 메모리 안의 오디오 재생
- 실제 MIDI 음표·가사 데이터를 사용한 녹음 안내 화면
- 실시간 마이크 파형, 음정 마커, 가사 진행 표시
- 녹음 중 왼쪽 카세트 릴과 테이프 애니메이션
- 녹음 후 사용자 이름을 분리된 J카드 TRACK 01에 기록
- 브라우저 안에서 반주와 녹음을 합친 WAV 렌더링
- `js/engines/`의 음역·파트 완주, 음색, 선호곡, MIDI, 곡 데이터 엔진
- 후보 1차 필터: 점수가 아니라 `complete === true`인지 확인
- 후보 정렬 참고값: 역할 35% + 음색 25% + 선호곡·장르 40%; 음색 점수가 없으면 50으로 대체
- 측정된 사용자 데이터와 기능 확인용 후보 예시 데이터를 화면에서 구분
- 음색을 Canvas PNG 데이터 URL로 생성하고, 기본 래스터 음색 이미지도 제공

## 클로드 전달본 대조 자료

- 원본 전체를 `references/claude-handoff/`에 참고 전용으로 보존했다.
- 반드시 `references/claude-handoff/README.md`를 먼저 읽고, 이어서 `엔진-명세.md`를 읽는다.
- 원본 `.mid`, 순수 엔진 JS, 기능별 HTML 화면도 들어 있다.
- 이 자료를 그대로 UI에 복사하지 않는다. 현재 구현과 기능 단위로 비교해 더 나은 쪽을 한 번만 반영한다.
- 특히 음역 측정, 녹음-반주 정렬, 믹싱, 음색 분석·이미지화, 곡·파트 완주 판정, 후보 정렬을 원본과 현재 코드 사이에서 각각 실행 검증한다.

## 아직 구현하지 않은 서버 영역

- 회원가입·로그인, 사용자 프로필과 동의/신고/차단
- 실제 음원/반주 라이선스 및 곡 카탈로그
- 녹음·영상 업로드, 영구 저장, 재생 URL, 삭제 정책
- 실제 사용자 음역·음색·선호곡·프로젝트 기록 저장
- 여러 후보에게 참여 제안 발송, 수락/거절, 수락자마다 프로젝트 복제
- 프로젝트 메시지와 상태 알림
- 서버/작업 큐 기반 믹싱, 실패 재시도, 전문가 의뢰 흐름
- 완성곡 하트 평가와 추천 피드백 반영
- 운영자 도구, 악성 콘텐츠 대응, 개인정보·저작권 정책

## 구현 시 주의

- `js/engines/*.js`는 전달받은 순수 UMD 엔진이며 함수 계약을 먼저 존중한다. 화면은 엔진 결과를 이해하기 쉬운 말로 번역한다.
- 음역 점수는 통과 기준이 아니다. 파트의 최저음과 최고음 모두 도달 가능한지를 먼저 확인한다.
- 음색과 취향 점수는 절대적인 정답이 아니라 추천 순서용 가설이다.
- 사용자의 실제 녹음에서 계산한 값과 샘플 후보의 설계값을 섞어 사실처럼 말하지 않는다.
- 클로드 프로토타입은 엔진과 계산식의 출처일 뿐이며 UI 원본이 아니다.
- 디자인 변경은 기존보다 명백히 나아질 때만 한다. 기능 추가 때문에 Linen의 밀도, 종이 질감, 카세트 비율, 이미지 크기를 후퇴시키지 않는다.

## Cloud Work 첫 목표

1. 현재 정적 앱을 그대로 실행해 회귀 기준을 확보한다.
2. 서버 포함 아키텍처와 데이터 모델을 문서화한다.
3. 로그인 없이도 가능한 데모 흐름을 유지하면서, 개발용 계정으로 실제 업로드 → 다중 제안 → 수락자별 프로젝트 생성 → 두 트랙 믹싱 → 하트 평가까지 한 수직 흐름을 구현한다.
4. 오디오 처리는 실패·시간 초과·재시도를 고려해 비동기 작업으로 분리한다.
5. 각 단계마다 모바일 화면 QA와 기능 테스트를 갱신한다.

## 검증

- `qa/run-audio-functional-qa.mjs`: 가짜 마이크로 실제 녹음 Blob, PNG 음색 이미지, WAV 믹스, 가로 넘침을 확인한다.
- 마지막 로컬 검증 결과: 모두 PASS.
