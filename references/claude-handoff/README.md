# 이음 — 인수인계

음역대로 사람을 매칭하고, 각자 녹음한 걸 듀엣·파트별로 합치는 모바일 웹앱.

## 폴더

```
엔진-명세.md      함수 쓰는 법. 이것부터 읽을 것
디자인-명세.md     색·간격·글자·부품. 그 다음 읽을 것
서버-설계.md       테이블 구조 · 무엇을 서버에 둘지
프롬프트.md        (사람용) Codex 에 넣을 말

match.js          음역으로 거르기 · 조옮김 · 파트 배정
timbre.js         목소리에서 음색·역할 뽑기
taste.js          취향 맞춰보기 · 추천 문구
midi.js           MIDI 읽기·쓰기 · 가사 시각
song.js           곡 데이터 · 화성 붙이기
                  ↑ 다섯 개는 화면을 전혀 안 건드리는 순수 함수. 고치지 말 것

밤 사이.mid        듀엣 2파트 · 가사 48자
마주.mid           SATB 4파트 · 가사 15자

화면/             돌아가는 프로토타입 7개. 보고 베낄 것
```

## 화면/ 폴더

| 파일 | 무엇 | 줄 | 검사 시작 | 상태 |
|---|---|---|---|---|
| `음역대측정.html` | 실제 마이크로 C2~C6 재기 | 317 | 303 | 잘 됨 |
| `매칭스와이프.html` | 상대 찾기 · 카드 넘기기 | 940 | 629 | 47/47 |
| `곡과가사.html` | 노래방 · 가사 흐르기 | 439 | 320 | 19/19 |
| `카세트J카드.html` | 카세트 릴 · J카드 기입 | 412 | 376 | 12/12 |
| `녹음싱크.html` | 녹음하고 반주에 맞추기 | 597 | — | **소리 문제 있음** |
| `파트궁합.html` | MIDI 곡의 파트 판정 | 359 | 356 | 잘 됨 |
| `mix-test.html` | 트랙 여러 개 합치기 | 83 | — | 7/7 |

`검사 시작` 줄부터 끝까지는 `window.__T()` 자동 검사입니다.
화면만 볼 거면 그 앞까지가 본체입니다.

### 성치 않은 하나

**`녹음싱크.html`** — **정렬 계산은 맞습니다.** 지연 137.0ms·92.0ms 를 정확히 찾고 잔차 −0.02ms.
문제는 소리를 다듬는 쪽입니다.
- 입력이 작으면(2% 수준) 정규화가 42배까지 키워서 지지직거립니다
- 기준음을 못 찾으면 잘라내는 지점이 틀려 목소리가 잘립니다
- AudioWorklet 이 안 되면 ScriptProcessor 로 떨어집니다

정렬 부분은 그대로 쓰고, 소리 다듬는 부분만 다시 보면 됩니다.

## 붙이는 순서

```
음역대측정 화면 ─┐
                 ├─> match.rankCandidates(나, 후보들, 곡, 7)
song.js 곡 ──────┘        │
                          ├─ complete === false  → 후보에서 뺀다 (예/아니오)
                          └─ complete === true   → 아래로
                                 │
    녹음 파형 ──> timbre.features ──> roles ──> rolePair ─┐
    앱 활동기록 ─> taste.tasteMatch ──────────────────────┤
                                                          ├─> 궁합
                                                          ┘
    곡 ──> midi.write ──> .mid
    곡 ──> midi.syncLyrics ──> 노래방 가사 (글자마다 초)
```

## 꼭 지킬 것

1. 엔진 `.js` 5개는 고치지 마세요. 값만 주고받습니다.
2. 1단계 거름은 **점수가 아니라 예/아니오**입니다. `complete` 를 보세요. 음역 `score` 는 거름망에 쓰지 마세요.
3. `blend.score` 는 `null` 일 수 있습니다. 보정 안 하면 궁합이 `NaN` 이 됩니다.
4. 375px 에서 가로로 넘치면 안 됩니다. `min-width` 금지.
5. **얼굴 사진을 쓰지 마세요.** 기획의 전제입니다. 지금은 음역 막대가 그 자리를 대신합니다.
   나중에 목소리에서 만든 이미지가 그 자리에 들어갈 예정입니다 (아직 없음).
6. 숫자는 `tabular-nums`.
7. 실측과 설계값을 같은 어조로 쓰지 마세요. `엔진-명세.md` 10번 참고.

## 아직 안 정한 것

`디자인-명세.md` 12번에 둘 있습니다. 거기 닿는 작업이면 먼저 물어보세요.

- 사람 1 의 색이 화면마다 다릅니다 (주황 / 파랑)
- `--ink-3`(#A79E93) 으로 본문을 쓰고 있는데 대비가 2.30:1 입니다

그리고 다크 컨셉·로고·손글씨가 들어갈 자리는 아직 안 골랐습니다.
지금 화면은 전부 종이(밝은) 쪽입니다.

## 서버

`서버-설계.md` 를 보세요. 요점만 —

- 엔진 5개는 **서버가 생겨도 안 고칩니다.** 브라우저에서 그대로 돕니다.
- 서버가 필요한 건 계산이 아니라 **주고받기**입니다 — 다른 사람 보기, 녹음 넘기기, 로그인.
- Supabase 를 쓰면 **서버 코드가 0줄**입니다. 테이블 6개만 만들면 됩니다.
- **오디오를 서버에서 분석하지 마세요.** 브라우저가 `timbre.features()` 를 돌려 숫자만 올립니다.
- **매칭 판정도 서버에서 하지 마세요.** 서버는 후보 100명만 추려 주고,
  `match.rankCandidates` 는 브라우저에서 돕니다.

## 없는 것

- **사람 목소리 데이터 0명.** 설계값은 실제 반응이 쌓이면 다시 맞춰야 합니다.
- 곡 2개뿐이고 제가 찍은 것입니다. 진짜 곡이 생기면 `song.js` 의 `BAM`·`MAJU` 만
  갈아끼우면 나머지 코드는 손댈 게 없습니다.
- 조옮김은 가단조 기준입니다.
