# 이음 모바일 웹 디자인 규정 v1.0

> 문서 상태: 구현 전 확정 디자인 기준  
> 작성일: 2026-09-17  
> 기준 화면: 430px 모바일 웹  
> 적용 대상: HTML, CSS, JavaScript 기반 MVP  
> 기획 기준: `ieum-student.md`

## 0. 문서 목적과 우선순위

이 문서는 `beginner-design-rull.md`의 레이아웃·위계·컴포넌트·접근성 상세도를 보존하면서, 이음의 음역 탐색과 비동기 합창 경험에 맞게 시각 언어를 전면 재구성한 구현 기준이다.

충돌 시 우선순위:

1. 접근성, 사용 안전, 실제 모바일 웹 동작
2. `ieum-student.md`의 사용자 확정 요구
3. 이 문서의 제품별 결정
4. 원본 `beginner-design-rull.md`의 공통 규칙
5. 화면별 명시적 예외

## 1. 디자인 방향

### 1.1 핵심 인상

- 공개 무대가 아닌 개인 녹음실의 집중감
- 음악 앱의 네온 대신 산화된 녹음 버튼과 청록색 레벨 미터
- 종이 J카드, 투명 플라스틱, 알루미늄, 자기테이프의 물성
- 얼굴 대신 음색 입자와 곡 편성의 추상 이미지
- 사람 관계보다 음역, 키, 파트, 마감과 제출 상태가 먼저 읽히는 구조
- 카세트 한 장면에만 강한 기억점을 두고 나머지 화면은 조용하게 정리

### 1.2 Pinterest E그룹에서 유지할 것

- 흑백 편집 디자인의 명확한 정렬
- 얇은 선으로 구분하는 기록지 구조
- 믹스테이프 J카드의 개인적인 흔적
- 종이와 카세트 재료의 질감
- 제한된 색을 신호로 쓰는 방식

### 1.3 바꿀 것

- 앱 전체에 낡은 종이 필터를 덮지 않는다.
- 모든 화면을 포스터처럼 만들지 않는다.
- 읽기 어려운 손글씨를 본문과 버튼에 사용하지 않는다.
- 같은 사각 카드와 작은 설명을 반복하지 않는다.
- 정적 카세트 사진을 진행 상태처럼 속이지 않는다.

### 1.4 고유한 한 장면

가장 대담한 시각 요소는 프로젝트룸의 작동하는 카세트다. 외부 테이프의 길이, 양쪽 릴 회전, J카드 필체가 제출 데이터에 따라 바뀐다. 다른 화면은 이 장면을 돋보이게 하기 위해 차분한 기록지 구조를 사용한다.

## 2. 레이아웃 시스템

### 2.1 기준 폭

| 구간 | 화면 폭 | 좌우 여백 | 콘텐츠 폭 |
|---|---:|---:|---:|
| Minimum | 360–389px | 16px | `100% - 32px` |
| Compact | 390–399px | 18px | `100% - 36px` |
| Reference | 400–430px | 18px | `100% - 36px` |
| Wide | 431px 이상 | 중앙 430px 셸 | 최대 394px |

- 앱 셸은 최대 430px이다.
- 의미 없는 페이지 전체 수평 스크롤을 금지한다.
- 곡 커버, 열린 파트 음색 이미지와 완성 콜라주만 필요할 때 풀블리드를 허용한다.
- Safe Area는 `env(safe-area-inset-*)`로 처리한다.

### 2.2 정렬

- 제목, 설명, 데이터와 버튼은 기본적으로 좌측 정렬한다.
- 음역 측정의 현재 음명과 녹음 시간만 중앙 정렬할 수 있다.
- 본문 한 줄 길이는 80자를 넘기지 않는다.
- 숫자와 음명은 열이 바뀌어도 흔들리지 않게 tabular numeral을 사용한다.

### 2.3 세로 간격

| 역할 | 값 |
|---|---:|
| Header 아래 첫 콘텐츠 | 28px |
| 홈 주요 섹션 사이 | 48px |
| 상세 화면 섹션 사이 | 40px |
| SectionHeader와 콘텐츠 | 16px |
| 카드 목록 사이 | 12px |
| 제목과 설명 | 8px |
| 설명과 행동 | 18px |

### 2.4 화면 골격

```text
AppShell 430
├─ AppHeader 또는 DetailHeader
├─ Main
│  ├─ PageIntro
│  ├─ PrimaryContent
│  ├─ Evidence / State
│  └─ NextAction
└─ BottomNavigation 또는 StickyActionBar
```

BottomNavigation과 StickyActionBar는 동시에 표시하지 않는다.

## 3. 컬러 토큰

```css
:root {
  --color-bg-app: #F1F0EC;
  --color-bg-paper: #FBFAF6;
  --color-bg-muted: #E7E5DF;
  --color-bg-deep: #191817;
  --color-bg-tape: #2A2927;

  --color-primary: #B6422F;
  --color-primary-pressed: #953426;
  --color-primary-soft: rgba(182, 66, 47, .10);

  --color-accent: #008E9B;
  --color-accent-pressed: #006F79;
  --color-accent-soft: rgba(0, 142, 155, .10);

  --color-text-primary: #191817;
  --color-text-secondary: #5D5A55;
  --color-text-muted: #77736C;
  --color-text-inverse: #FBFAF6;

  --color-border: #A7A39B;
  --color-border-subtle: #D7D5CE;
  --color-focus: #008E9B;
  --color-success: #2F6B55;
  --color-danger: #B6422F;
}
```

### 3.1 사용 규칙

- Primary는 녹음, 제출, 참여 확정, 믹싱 생성에 사용한다.
- Accent는 음역, 입력 레벨, 선택된 측정 상태에 사용한다.
- 같은 요소에 두 유채색을 동시에 장식으로 사용하지 않는다.
- 밝은 보조 면은 회색 계열만 사용한다.
- 성공색은 완료 상태에만 제한한다.
- 상태는 색상과 텍스트·아이콘을 함께 사용한다.

## 4. 타이포그래피

### 4.1 글꼴 역할

```css
:root {
  --font-ui: "Pretendard Variable", "Pretendard", "Noto Sans KR", sans-serif;
  --font-hand-a: "Nanum Pen Script", cursive;
  --font-hand-b: "Single Day", cursive;
}
```

- 일반 UI와 한글 본문은 산세리프다.
- 음명, Hz, 시간, 진행률은 UI 글꼴의 tabular numeral을 사용한다.
- 손글씨 A/B는 J카드 제출 기록과 날짜에만 사용한다.
- 궁서체, 명조체, 붓글씨, 기본 cursive를 대체 글꼴로 사용하지 않는다.

### 4.2 타입 스케일

| 역할 | 크기/두께 | 행간 |
|---|---|---:|
| 측정 음명 | 72px/700 | 1 |
| 페이지 제목 | 30px/700 | 1.2 |
| 섹션 제목 | 24px/700 | 1.25 |
| 카드 제목 | 20px/600 | 1.3 |
| 본문·CTA | 16px/400–600 | 1.5 |
| 라벨 | 14px/500–600 | 1.4 |
| 메타 | 12px/500 | 1.4 |
| J카드 손글씨 | 18–24px/Regular | 1.25 |

- 일반 텍스트는 최소 12px이다.
- 긴 제목 때문에 글자 크기를 줄이지 않고 줄 수를 제한한다.
- 추적을 과하게 벌린 영문 대문자 라벨을 반복하지 않는다.

## 5. 간격·라운드·선·그림자

```css
:root {
  --space-1: 2px;
  --space-2: 4px;
  --space-3: 6px;
  --space-4: 8px;
  --space-5: 10px;
  --space-6: 12px;
  --space-7: 14px;
  --space-8: 16px;
  --space-9: 18px;
  --space-10: 20px;
  --space-12: 24px;
  --space-16: 32px;
  --space-20: 40px;
  --space-24: 48px;

  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-card: 12px;
  --radius-media: 16px;
  --radius-modal: 24px;
  --radius-pill: 999px;
}
```

- 편집지와 J카드: 0–4px의 단정한 모서리
- 일반 카드: 12px
- 이미지 카드: 16px
- 모달: 24px
- 버튼과 칩: pill
- 그림자는 플로팅 내비게이션과 모달에만 사용한다.
- 카드 구분은 대부분 선, 간격과 배경 차로 해결한다.

## 6. 아이콘과 모션

### 6.1 아이콘

- 기능 아이콘은 Font Awesome Free 한 계열로 통일한다.
- 기본 시각 크기 20–24px, 터치 영역 최소 44×44px.
- 이모지, CSS 도형, 텍스트 특수문자, 임의 기능 SVG를 사용하지 않는다.
- 카세트 릴, 음역 레일과 파형은 기능 아이콘이 아닌 데이터 시각화이므로 CSS/SVG 구현을 허용한다.

### 6.2 모션

| 동작 | 시간 |
|---|---:|
| 눌림 | 120ms |
| 선택·미터 변화 | 220ms |
| 화면·모달 | 280ms |
| 릴 감기 | 900–1400ms |

- 비사용자 유발 장식 애니메이션을 반복하지 않는다.
- 릴 회전은 트랙 제출 직후에만 실행한다.
- `prefers-reduced-motion`에서는 회전을 제거하고 상태를 즉시 변경한다.

## 7. 공통 내비게이션

### 7.1 AppHeader

- 실제 높이 52px, 좌우 패딩 18px.
- 좌측 텍스트 로고 `이음`, 우측 알림·저장 중 최대 2개.
- 로고는 본문과 같은 산세리프를 사용하며 장식 손글씨를 사용하지 않는다.

### 7.2 DetailHeader

- 높이 최소 52px.
- 왼쪽 뒤로 가기, 중앙 한 줄 제목, 오른쪽 선택 행동.
- 양쪽 버튼은 44×44px.
- 브라우저 history와 같은 기록을 사용한다.

### 7.3 BottomNavigation

- 최대 폭 398px, 최소 높이 78px, 앱 셸 하단 중앙.
- 4개 탭: 홈, 곡, 프로젝트, 내 목소리.
- 활성 탭 하나만 Accent 또는 Primary로 표시한다.
- 하단 safe area를 포함하고 본문을 가리지 않는다.

### 7.4 StickyActionBar

- 앱 셸 하단 전체 폭, 최소 높이 84px + safe area.
- 단일 CTA 또는 동일 폭 이중 CTA.
- BottomNavigation과 함께 표시하지 않는다.

## 8. 버튼·칩·상태 컨트롤

### 8.1 Button

| Variant | 높이 | 사용 |
|---|---:|---|
| Primary | 48px | 다음 단계, 제출, 완성 |
| Secondary | 48px | 다시 듣기, 비교 |
| Record | 72px | 녹음 시작·정지 |
| Compact | 36px/44px hit | 카드 내부 행동 |
| Text | 44px hit | 다시 측정, 취소 |
| Icon | 44×44px | 뒤로, 재생, 저장 |

- 문구는 누른 뒤 결과가 드러나는 동사형으로 쓴다.
- Default, Pressed, Focus, Disabled, Loading을 구현한다.
- Loading에서도 크기가 변하지 않는다.

### 8.2 FilterChip

- 시각 높이 34–38px, 실제 터치 높이 44px.
- 선택 상태는 Accent 배경과 명확한 텍스트로 표시한다.
- 곡 키, 편성, 파트, 음역 적합도 필터에 사용한다.

### 8.3 StatusBadge

- 12–14px, 실제 의미가 있는 상태에만 사용한다.
- 예: `원키 적합`, `-1키 권장`, `녹음 필요`, `모든 트랙 준비`.
- 장식용 순번과 추상 등급을 금지한다.

## 9. 음역·음색 컴포넌트

### 9.1 RangeFeature

- 홈 최상단의 가장 큰 기능 영역.
- 미측정: 발성 전 안내와 `음역 측정 준비하기` CTA.
- 측정 완료: 전체 범위, 안정 구간, 마지막 측정일, `결과 다시 보기`.
- 카세트나 협업 슬로건보다 먼저 표시한다.

### 9.2 PitchRail

- 음명 축, 전체 범위, 안정 구간, 파트 요구 범위를 겹쳐 보여준다.
- 색상만이 아니라 라벨과 수치로 범위를 구분한다.
- 가로 스크롤 없이 360px 안에 들어온다.

### 9.3 LivePitchMeter

- 현재 음명 72px.
- 입력 레벨, 감지 상태, 단계 1/3을 함께 표시한다.
- 감지 실패 시 원인과 재시도 행동을 같은 자리에서 제공한다.

### 9.4 TimbreSpecimen

- 얼굴 대신 생성된 음색 이미지와 2–3개 관찰값을 제공한다.
- 고정 성격이나 성별을 추론하지 않는다.
- 이미지가 없어도 텍스트 구조가 유지된다.

## 10. 곡과 파트 카드

### 10.1 SongHeroCard

- 394×250px, 이미지 비율 16:10, 라운드 16px.
- 제목 최대 2줄, 키와 추천 이유는 별도 정보 면에 둔다.
- 텍스트를 이미지에 겹치면 하단 제한 Scrim을 사용한다.

### 10.2 SongListRow

- `92px minmax(0,1fr) 44px` 3열.
- 이미지 92×92px, 제목 1줄, 추천 이유 2줄.
- 재생과 상세 이동을 별도 버튼으로 구분한다.

### 10.3 PartOptionRow

- 파트명, 필요 음역, 키 변화, 중첩 근거를 한 행에 표시한다.
- 성별이 아니라 실제 구간을 우선한다.
- 선택 상태는 테두리, 아이콘과 문구로 함께 전달한다.

### 10.4 OpenPartCard

- 394×520px 안에서 이미지 4:3과 데이터 면을 결합한다.
- 제목은 곡과 파트, 사람 이름은 보조 기록에만 둔다.
- 필요한 음역, 목표일, 현재 트랙, 음색 참고, 음성 재생을 제공한다.
- 오른쪽 제스처는 다음 카드, 아래 제스처는 상세.
- 같은 행동의 버튼과 ArrowRight/ArrowDown 대체를 제공한다.
- 하트, X, 매치 성공 효과를 사용하지 않는다.

## 11. 프로젝트와 카세트 컴포넌트

### 11.1 CassetteProgress

- 실제 DOM/CSS/SVG로 구성하며 정적 이미지 한 장으로 대체하지 않는다.
- 카세트 본체, 왼쪽 릴, 오른쪽 릴, 중앙 창, 외부 테이프, J카드로 나눈다.
- 진행률은 `0`, `25`, `50`, `75`, `100`을 지원한다.

| 진행 | 외부 테이프 | 동작 |
|---:|---|---|
| 0 | 가장 길게 노출 | 정지 |
| 25 | 조금 감김 | 왼쪽 릴 |
| 50 | 절반 감김 | 왼쪽 릴 |
| 75 | 대부분 감김 | 오른쪽 릴 |
| 100 | 완전히 내부로 | 오른쪽 릴 후 정지 |

### 11.2 JCard

- 파트별 행, 제출자, 날짜, 상태를 표시한다.
- 두 제출자의 기록은 서로 다른 손글씨를 사용한다.
- 구조 라벨과 상태는 UI 산세리프를 유지한다.
- 손글씨가 없어도 정보가 읽히도록 접근 가능한 텍스트를 제공한다.

### 11.3 TrackStatusRow

- 트랙 번호, 파트, 음역, 제출 상태, 재생 버튼.
- 번호는 순서 데이터이므로 사용 가능하다.
- 상태 변경 뒤 관련 카세트와 CTA가 동시에 갱신된다.

## 12. 녹음·믹싱 컴포넌트

### 12.1 Recorder

- 가이드 트랙, 카운트인, 입력 레벨, 시간, 시작·정지 버튼.
- 권한 거부, 입력 없음, 녹음 중, 미리듣기, 제출 중, 완료 상태.
- 버튼만으로 결과를 알 수 있게 `녹음 시작`, `녹음 멈추기`, `이 트랙 제출하기`로 쓴다.

### 12.2 MixChoice

- AI 믹싱과 전문가 의뢰를 동일한 사각 카드 복제로 끝내지 않는다.
- AI는 빠른 처리 시간과 자동 범위를 미터형 패널로 보여준다.
- 전문가는 요청 메모와 견적 절차를 기록지형 패널로 보여준다.
- 선택 뒤 시간, 수정, 비용과 다음 행동이 갱신된다.

## 13. 카드뉴스형 콘텐츠 규칙

- 이미지가 의미를 전달하는 곡·편성·음색 콘텐츠에는 생성 이미지를 반드시 사용한다.
- Hero형, 가로 목록형, 세로 오버레이형, 비교형을 역할에 따라 구분한다.
- 같은 라운드 박스 안에 아이콘·제목·설명만 반복하지 않는다.
- 이미지 비율, 안전 영역, 제목 최대 줄 수와 클릭 행동을 문서에 명시한다.
- 설정, 데이터 표, 입력 폼에는 이미지를 억지로 넣지 않는다.

## 14. 이미지와 Overlay

- 신규 로컬 WebP 10장만 사용한다.
- 얼굴, 인물 프로필, 무대, 관객, 입술 접사를 금지한다.
- `object-fit: cover`와 이미지별 `object-position`을 지정한다.
- 텍스트가 이미지 위에 있을 때 텍스트 영역에만 Scrim을 사용한다.
- 이미지 실패 시 비율과 텍스트 구조를 유지한다.
- 첫 핵심 이미지만 우선 로드하고 나머지는 lazy load한다.

## 15. 화면별 조합

### 홈

AppHeader → RangeFeature → 곡 Rail → 열린 파트 Preview → 편성 이미지 → 진행 프로젝트 → 최근 완성 → BottomNavigation.

### 음역 안내·측정·리포트

DetailHeader → 한 화면 목적 → 측정/결과 → 안전·오류 상태 → StickyActionBar.

### 곡 탐색·상세

Header → 실제 필터 → 이미지 목록 → 음역 근거 → 다음 행동.

### 열린 파트·궁합·조건

사람보다 곡과 파트 → 음색 이미지 → 범위·마감·완주 근거 → 조건 동의.

### 프로젝트·녹음·믹싱

프로젝트 상태 → 작동 카세트/J카드 → 실제 다음 행동 → 완료 결과.

## 16. 상태 UI

### Loading

- 300ms를 넘는 전환에서 실제 구조와 같은 Skeleton을 사용한다.
- 버튼 Loading에서 크기를 유지하고 중복 제출을 막는다.

### Empty

- 빈 이유와 다음 행동을 함께 제공한다.
- 예: `이 조건에 맞는 곡이 아직 없어요. 키 범위를 넓혀 다시 찾아보세요.`

### Error

- 원인과 복구 행동을 함께 쓴다.
- 측정 실패가 전체 내비게이션을 막지 않는다.

### Toast

- 측정 저장, 조건 참여, 트랙 제출과 믹싱 완료를 구체적으로 알린다.
- Live Region으로 전달한다.

### Modal

- 프로젝트 나가기, 트랙 삭제처럼 되돌리기 어려운 행동에만 사용한다.
- Focus Trap, Escape, 배경 제어, 포커스 복귀를 지원한다.

## 17. 콘텐츠 규칙

| 요소 | 제한 |
|---|---|
| 화면 제목 | 1줄, 불가피하면 2줄 |
| 섹션 제목 | 1줄 |
| 카드 제목 | 1–2줄 |
| 카드 설명 | 최대 2줄 |
| CTA | 한 줄, 결과가 드러나는 동사형 |
| 메타 | 기본 한 줄 |

- 추상 홍보 문구를 사용하지 않는다.
- `확인`, `시작`, `제출`만 단독으로 쓰지 않는다.
- 같은 기능의 이름을 모든 화면에서 통일한다.
- `J카드`와 `카세트`를 브랜드 슬로건처럼 반복하지 않는다.

## 18. 접근성

- 논리 화면마다 H1 하나.
- 제목 단계를 건너뛰지 않는다.
- 모든 상호작용은 키보드로 가능해야 한다.
- Focus Ring 최소 2px.
- 터치 영역 최소 44×44px, 인접 영역 8px 권장.
- 일반 본문 대비 4.5:1, 큰 텍스트와 경계 3:1 이상.
- 200% 확대에서 핵심 흐름을 완료할 수 있어야 한다.
- Carousel과 스와이프는 현재 위치와 버튼 대체를 제공한다.
- 상태 변화는 Live Region으로 알린다.
- 모션 감소 설정을 존중한다.

## 19. 구현 규칙

- 링크 이동은 `a`, 상태 변경은 `button`.
- 클릭 가능한 `div`를 만들지 않는다.
- 카드 전체 링크 안에 별도 버튼을 중첩하지 않는다.
- 목록은 `ul/ol/li`, 날짜는 `time`을 우선 검토한다.
- 토큰을 사용하고 재사용 컴포넌트에 페이지 값을 하드코딩하지 않는다.
- `100dvh`를 사용한다.
- 앱 전체에 `overflow-x:auto`를 적용하지 않는다.
- 화면·측정·녹음·프로젝트 상태는 하나의 저장 모델을 사용한다.
- UI 재렌더 뒤 이벤트를 중복 등록하지 않는다.

## 20. 금지 규칙

- 얼굴 프로필 카드
- 소개팅형 하트·X·매치 연출
- 보라·핑크·네온 음악 앱 팔레트
- 궁서체·명조체·붓글씨 손글씨 대체
- 정적 카세트 진행 이미지
- 텍스트만 든 동일 사각 카드 반복
- 장식용 영문 대문자 라벨 남용
- 의미 없는 `01/02/03`
- 기능 없는 버튼
- 정지 기능 없는 자동 Slider
- BottomNavigation과 StickyActionBar 중복
- 외부 이미지 URL
- iOS 상태바와 Home Indicator 모사

## 21. QA 체크리스트

### 화면

- 360·390·430px에서 수평 넘침 0.
- 431px 이상에서 430px 셸 중앙 정렬.
- 고정 UI가 콘텐츠를 가리지 않음.
- 동일 카드 복제보다 역할별 구도 차이가 보임.

### 기능

- 12화면 이동과 브라우저 뒤로 가기.
- 측정, 필터, 스와이프, 조건, 녹음, 제출, 믹싱 흐름.
- 새로고침 뒤 핵심 상태 유지.
- 빈 상태, 로딩, 오류, 완료 상태.
- 0/50/100과 25% 확장 카세트 상태.

### 이미지

- 로컬 WebP 정확히 10장.
- 10장 모두 실제 사용.
- 깨진 이미지와 외부 URL 0.
- 얼굴과 읽을 수 있는 생성 글자 0.

### 접근성

- 44px 터치 영역.
- 키보드 핵심 흐름.
- 명확한 포커스.
- 적절한 대체 텍스트.
- 200% 확대와 모션 감소.

## 22. 완료 정의와 변경 관리

디자인 구현은 다음을 모두 만족해야 완료다.

- 이 문서의 토큰과 컴포넌트를 사용한다.
- `ieum-student.md`의 변경 금지 항목을 위반하지 않는다.
- 12화면과 모든 핵심 상태가 구현된다.
- 신규 이미지 10장이 적용된다.
- 최소 3회 브라우저 회귀 테스트를 통과한다.
- 문서와 코드의 색상·타입·간격·행동 이름이 일치한다.

토큰 삭제, 구조 변경, 접근성 기준 변경은 Major 변경이다. Variant 추가와 문구 보완은 Minor 변경이며 날짜·이유·영향 범위를 기록한다.

### 변경 이력

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| 1.0 | 2026-09-17 | 학생 기획과 B 색상을 반영해 이음 전용 디자인 규정 작성 |

