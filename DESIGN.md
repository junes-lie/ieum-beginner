# 이음 Linen Tape 디자인 기준

## 선택

주 디자인 기반은 `Linen Press DESIGN.md`다. 따뜻한 종이 면, 잉크색 텍스트, 얇은 경계와 낮은 시각 소음이 이음의 안전하고 진지한 소규모 협업에 가장 잘 맞는다.

`Cassette DESIGN.md`의 전면 검정·형광 노랑과 `Vinyl Noir DESIGN.md`의 전면 다크 무대는 음악성은 강하지만 공연·인기·야간 스트리밍의 인상을 키우므로 전체 시스템으로 사용하지 않는다. 카세트의 장비 표기와 릴 움직임만 제품 고유 인터랙션으로 가져온다.

## 토큰

| 역할 | 값 | 사용 |
|---|---|---|
| Linen paper | `#F7F2E8` | 앱 바탕, 주요 기록 면 |
| Pressed paper | `#EFE9DF` | 보조 면, 선택 전 컨트롤 |
| Carbon ink | `#1D1A16` | 제목, 본문, 주 행동 |
| Pencil | `#6E6760` | 설명, 메타 정보 |
| Cognac oxide | `#A35828` | 녹음·재생 위치·포커스만 |
| Tape brown | `#382A22` | 자기테이프와 카세트 내부 |

- 일반 UI: Pretendard 또는 Noto Sans KR 계열 산세리프
- 곡명과 숫자 표기: Georgia 계열 세리프를 제한적으로 사용
- J카드 제출자 A/B: 제공된 두 손글씨 폰트만 사용
- 궁서체·기본 cursive·본문 손글씨는 사용하지 않는다.

## 레이아웃

모든 정보는 왼쪽 정렬을 기본으로 한다. 카드보다 종이 면, 얇은 선, 줄 간격으로 구조를 만든다. 둥근 모서리는 조작 요소와 이미지에만 제한한다.

```text
홈
┌ 헤더 ───────────────────────┐
│ 음역 측정 종이 면            │
├─────────────────────────────┤
│ 추천 곡 가로 레일            │
│ 열린 파트 / 프로젝트 기록     │
└ 하단 내비게이션 ─────────────┘

녹음 중
┌ 곡명 · 파트 · 시간 ──────────┐
│ 곡별 스킨 카세트              │
│  ◉ 왼쪽 릴 회전  ○ 오른쪽 릴 │
├─────────────────────────────┤
│ 이전 가사                     │
│ 현재 가사                     │
│ 다음 가사                     │
├─────────────────────────────┤
│ 목표 음정 바 + 현재 음 커서   │
│ 입력 레벨                     │
└ 녹음 멈추기 ─────────────────┘

녹음 후
┌ 정지한 카세트 + J카드 ───────┐
│ TRACK 01  민서 · 알토 응답     │
│ 녹음 길이 / 음정 안정도        │
└ 다시 녹음 / 트랙 제출 ───────┘
```

## 고유 원칙

1. 강한 시각적 기억점은 녹음 화면의 카세트 한 곳에 집중한다.
2. 카세트 스킨은 곡 이미지에서 가져오되 글자를 이미지에 합성하지 않는다.
3. 녹음 중 왼쪽 릴과 테이프만 움직인다. 미리듣기에서는 정지한다.
4. 가사와 음정 바는 장식이 아니라 녹음을 돕는 핵심 도구다.
5. 이름은 녹음 완료 뒤 계정 정보에서 자동으로 J카드에 기록한다. 녹음 화면에서 이름을 다시 묻거나 필체를 미리 보여주지 않는다.
6. 다른 화면은 기록지처럼 차분하게 유지해 녹음 장면의 의미를 보존한다.

## 홈 상태 전환

- 첫 사용 또는 측정 결과가 없을 때만 홈 상단에서 음역 측정을 권한다.
- 측정이 끝난 뒤에는 홈에서 측정 카드와 재측정 버튼을 제거하고, 곡 추천이 첫 콘텐츠가 된다.
- 저장된 음역 결과와 재측정은 `내 목소리` 화면에서 확인한다. 홈에서는 추천에 필요한 짧은 기준만 보인다.

## 공통 카세트 계약

- 홈 추천, 곡 상세, 참여 조건, 프로젝트, 녹음, 믹싱 화면은 모두 동일한 `.song-cassette` 본체를 사용한다.
- 카세트 본체 안에는 해당 곡의 실제 `.cassette-skin-image`를 넣는다. 화면과 상태에 따라 달라지는 것은 이 이미지, 테이프 진행률, 릴의 움직임뿐이다. 카세트의 비율·릴·테이프 창·나사 위치는 바꾸지 않는다.
- 곡명, 파트명, 참여자 이름, 날짜를 카세트 위에 인쇄하거나 겹쳐 놓지 않는다.
- J카드는 항상 카세트 다음에 놓이는 별도 종이 요소다. `.song-cassette` 내부나 카세트 이미지 위에 포함하지 않는다.
- 녹음 완료 시 J카드의 첫 줄에만 사용자 이름과 파트를 기입한다. 두 번째 줄은 다른 파트가 도착하기 전까지 빈 상태로 둔다.

## 자기검토

초안의 크림색·세리프 조합은 일반적인 편집형 웹 템플릿으로 보일 위험이 있었다. 이를 피하기 위해 한글 UI에서는 세리프를 주역으로 쓰지 않고, 자기테이프 선·곡별 스킨·실시간 음정 레일·상태에 따라 쓰이는 J카드를 구조적 언어로 삼았다. 색상보다 녹음 상태 변화가 브랜드를 설명하도록 수정했다.
